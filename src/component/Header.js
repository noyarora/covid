import React from 'react';

const Header = () => {
    return (
        <header>
            <h2 className="ui header">
                Patrick
            </h2>
        </header>
    );
}

export default Header;